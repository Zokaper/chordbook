{
  "name": "workspace",
  "version": "0.0.0",
  "homepage": "https://chordbook.zokaper.cc/",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "preinstall": "node scripts/ensure-pnpm.cjs",
    "dev": "corepack pnpm --filter @workspace/songbook run dev:web",
    "dev:web": "corepack pnpm --filter @workspace/songbook run dev:web",
    "preview:web": "corepack pnpm --filter @workspace/songbook run preview:web",
    "build:web": "corepack pnpm --filter @workspace/songbook run build:web",
    "cloudflare:login": "wrangler login",
    "deploy:web": "corepack pnpm run build:web && wrangler pages deploy artifacts/songbook/dist --project-name chordbook --branch main",
    "test": "corepack pnpm --filter @workspace/songbook run test",
    "test:watch": "corepack pnpm --filter @workspace/songbook run test:watch",
    "check": "corepack pnpm run typecheck:app && corepack pnpm run test",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "typecheck:app": "corepack pnpm --filter @workspace/songbook run typecheck",
    "typecheck:libs": "tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2",
    "wrangler": "^4.113.0"
  }
}
